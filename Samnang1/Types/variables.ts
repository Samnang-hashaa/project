let movieTitle: string ="Thor";
movieTitle="Wallstreets"

// numCatlives
let numCatLives: number =9;
numCatLives += one;

let gameOver: boolean =false;
gameOver= true;
gameOver="true";

//type inference refers to the javascript compiler's abilitu to infer types from certain values in your code.
let tvShow="Oliver kitteridge";
tvShow="The other Two";
tvShow=false;

let isFunny= false;
isFunny=true;
isFunny="asd";

//Any type is an escape hatch! it turns off type checking for the variable so you can do your thing \.

let thing : any="Hello";
thing=1;
thing=false;
thing=['apple','orange','mango'];

// Delayed Initialization & Implicit any
const movies =["spider-man","Thor","Iron man","Arrival"]
let foundMovie;

for(let movies of movies){
    if(movie === "spider-man"){
        foundMovie="spider-man";
        
    }
}
